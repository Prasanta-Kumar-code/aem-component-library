(function (document) { 'use strict'; document.querySelectorAll('.aemcl-faq').forEach(function (faq) { var search = faq.querySelector('.aemcl-faq__search'); if (search) { search.addEventListener('input', function () { var term = search.value.toLowerCase(); faq.querySelectorAll('.aemcl-faq__item').forEach(function (item) { item.hidden = item.dataset.question.toLowerCase().indexOf(term) === -1; }); }); } }); }(document));