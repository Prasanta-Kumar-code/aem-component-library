(function (document) { 'use strict'; document.querySelectorAll('.aemcl-tabs').forEach(function (tabs) { var buttons = tabs.querySelectorAll('[data-tab-index]'); var panels = tabs.querySelectorAll('[data-tab-panel]'); function select(index) { buttons.forEach(function (button) { button.setAttribute('aria-selected', String(button.dataset.tabIndex === String(index))); }); panels.forEach(function (panel) { panel.hidden = panel.dataset.tabPanel !== String(index); }); } buttons.forEach(function (button) { button.addEventListener('click', function () { select(button.dataset.tabIndex); }); }); select(0); }); }(document));